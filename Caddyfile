{
    email tanmay@tanmaymirgal.dev
    on_demand_tls {
        ask http://localhost:3000/api/caddy/ask
        interval 2m
        burst 5
    }
}

# The main dashboard
orb.tanmaymirgal.dev {
    reverse_proxy localhost:3000
}

# Deployed Projects (Dynamic subdomains - individual certs via HTTP-01)
:443 {
    tls {
        on_demand
    }
    reverse_proxy localhost:8000
}
